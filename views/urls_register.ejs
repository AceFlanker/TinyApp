<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>Sign Up | TinyApp</title>
</head>

<body>

  <%- include('partials/_header') %>

  <main style="margin: 1em;">
    <h3>Create an account</h3>
    <form action="/register" method="POST" id="register">
      <div class="form-group" style="margin:0px">
        <label for="email_register">Email address</label>
        <br>
        <input form="register" class="col-sm-3 my-1" type="email" name="email" class="form-control" id="email_register" placeholder="Enter email" value="<%= queryEmail %>">
        <p style="font-size:12px;color:red;margin:0px 17px;padding:0px">

          <% if (empty === 1) { %>
          <span>
            Can't be empty
          </span>
          <% } %>
          <% if (empty === 3) { %>
          <span>Email already exists</span>
          <% } %>
          <% if (empty !== 1 || empty !==3) { %>
            <span style="visibility: hidden">You found me! I'm a very important place holder, like literally. And althought I have a lot of stories to tell, but not today! My job right now is to hold this spot, so things look nice!</span>
          <% } %>
        </p>
      </div>
      <div class="form-group">
        <label for="password_register">Password</label>
        <br>
        <input form="register" class="col-sm-3 my-1" type="password" name="password" class="form-control" id="password_register" placeholder="Password">
        <p style="font-size:12px;color:red;margin:0px 17px;padding:0px"><span <% if (empty !== 2) { %> style="visibility:hidden" <% } %>>Can't be empty</span></p>
      </div>
      <button form="register" type="submit" class="btn btn-primary">Register</button>
    </form>
    <br>
    <p>Already have an account? <a href="/login">Sign in.</a></p>
  </main>
    <!-- Bootstrap JS -->
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
</body>
</html>